# HASHIFY-File-Integrity-Verifier
HASHIFY is a secure file integrity verification system using SHA-256 hashing. It generates a unique hash for a file and detects any tampering by comparing hash values. The project features a modular backend, detailed TXT/JSON reports, and a GUI-ready design for reliable verification.
